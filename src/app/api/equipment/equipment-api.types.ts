export interface EquipmentDto {
  name: string;
  description: string;
  properties: string;
}

export interface Equipment {
  id: number;
  name: string;
  description: string;
  properties: string;
}
